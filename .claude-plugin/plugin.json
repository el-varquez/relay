{
  "name": "relay",
  "displayName": "Relay",
  "version": "3.0.0",
  "description": "Give it a task; a relay of agents plans it (you approve), then builds, tests, and ships — Plan → Build → Test → Engineer Review → Ship, auto-looping on failure. Project-agnostic: reads each project's own CLAUDE.md, plus an optional relay.config.json. Stops at the PR by default and resumes when QA rejects.",
  "author": { "name": "el-varquez" },
  "homepage": "https://github.com/el-varquez/relay",
  "repository": "https://github.com/el-varquez/relay",
  "license": "MIT",
  "keywords": ["workflow", "pipeline", "agents", "orchestration", "relay", "build", "test"]
}
